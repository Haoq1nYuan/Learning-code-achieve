#include <iostream>
#include <algorithm>
#include <limits.h>
#include <vector>

using namespace std;
const int N = 100010;

string A, B;
int n[N], m[N], ans[N], k;

int main ()
{
    cout << (1 + 2 >> 2);
    
    return 0;
}