#include <iostream>
#include <algorithm>
#include <limits.h>
#include <vector>

using namespace std;
const int N = 100010;

string A, B;
int n[N], m[N], ans[N], k;

int main ()
{
    auto q = INT_MAX;
    
    return 0;
}