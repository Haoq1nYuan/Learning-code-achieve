#include <iostream>

using namespace std;

int n, m;

int main ()
{
    cin >> n >> m;

    while (n--)
    {
        int x, y;
        cin >> x >> y;
    }
    while (m--)
    {
        int a, b;
        cin >> a >> b;
    }

    cout << 4 << endl;

    return 0;
}