#include<stdio.h>
#define N 50000

int r, j, number;
long n, m, i, count;
long long lei[50010][3], jian[50010][3];

int main ( int argc, char *argv[] ) 
{
    scanf( "%ld %ld", &n, &m );
    for ( i = 0; i < n; i++ ) 
        scanf( "%lld %lld %d", &lei[i][0], &lei[i][1], &lei[i][2] );
    for ( i = 0; i < m; i++ ) 
        scanf( "%lld %lld %d", &jian[i][0], &jian[i][1], &jian[i][2] );
    for ( i = 0; i < m; i++ ) {
        
    }
    return 0;
}